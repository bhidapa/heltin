directive @refetchable(queryName: String!) on FRAGMENT_DEFINITION

directive @relay(mask: Boolean!) on FRAGMENT_SPREAD
# TODO: vscode-graphql does not like directive overloads
# directive @relay(plural: Boolean!) on FRAGMENT_DEFINITION

directive @connection(key: String!) on FIELD
directive @inline on FRAGMENT_DEFINITION

enum RequiredAction {
  NONE
  LOG
  THROW
}
directive @required(action: RequiredAction!) on FIELD
